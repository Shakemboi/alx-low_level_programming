#include <stdio.h>
#include <stdlib.h>

/**
 * mod - returns remainder of two numbers
 * @a: first parameter
 * @b: second parameter
 * Rturn: remainder of two numbers
 */
int mod(int a, int b)
{
	return (a % b);
}
