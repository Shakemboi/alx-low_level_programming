#include <stdio.h>
#include <stdlib.h>

/**
 * sub - subtracts two numbers
 * @a: first parameter
 * @b: second parameter
 * Rturn: subtraction of two numbers
 */
int sub(int a, int b)
{
	return (a - b);
}
