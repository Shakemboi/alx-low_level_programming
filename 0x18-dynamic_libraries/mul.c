#include <stdio.h>
#include <stdlib.h>

/**
 * mul - multiplies two numbers
 * @a: first parameter
 * @b: second parameter
 * Rturn: multiplication of two numbers
 */
int mul(int a, int b)
{
	return (a * b);
}
